package general;

public class ImageNormalizer {
	/**
	 * Normalizes an image for the use of a graph
	 * gradient, color conversion, cropping, etc.
	 * save as another file or as a "sequence" for graphs
	 * but I am not sure what a graph uses...
	 * well, what a graph uses depends on me, I guess
	 * 
	 * contain static methods only? 
	 * 
	 * @param args
	 */
	

	public static void main(String[] args) {
		// TODO Auto-generated method stub
		if (args.length < 1) {
		      System.err.println("You must provide either a directory or a file");
		      System.exit(1);
		}
	}

}
